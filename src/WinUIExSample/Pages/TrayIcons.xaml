<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIExSample.Pages.TrayIcons"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIExSample.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Background="{ThemeResource SemiTransparentBackgroundBrush}" CornerRadius="10" Padding="10" Grid.ColumnSpan="2">
            <TextBlock Text="Tray Icons" FontSize="32" FontFamily="Segoe UI" FontWeight="Light" Margin="0,-10,0,0" />
        </StackPanel>


        <StackPanel Grid.Row="1">
            <StackPanel Background="{StaticResource SemiTransparentBackgroundBrush}" CornerRadius="10" Padding="10" Margin="0,10,0,0">
                <ToggleSwitch Header="MainWindow: Is Visible in Tray" IsOn="{x:Bind MainWindow.IsVisibleInTray, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Background="{StaticResource SemiTransparentBackgroundBrush}" CornerRadius="10" Padding="10"  Margin="0,10,0,0">
                <TextBlock Text="Additional Tray Icons" FontSize="18" />
                <TextBox Text="TrayIcon Tooltip" PlaceholderText="Tooltip for icon" MaxLength="128" HorizontalAlignment="Left"
                         TextChanged="TextBox_TextChanged" x:Name="tooltip" Header="Tooltip" Width="150" />
                <ComboBox x:Name="iconSelector" Header="Icon:" Width="150" SelectedIndex="0" SelectionChanged="iconSelector_SelectionChanged">
                    <ComboBoxItem>OK</ComboBoxItem>
                    <ComboBoxItem>Error</ComboBoxItem>
                </ComboBox>
                <Button Content="Add TrayIcon" Click="CreateTray_Click" Width="150" />
                <Button Content="Clear Tray Icons" Click="ClearTray_Click" Width="150" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
